{
    "name": "desci-sentinel",
    "version": "1.0.0",
    "private": true,
    "description": "DeSci Sentinel monorepo — Backend + Frontend",
    "scripts": {
        "postinstall": "cd bio-scholar-backend && npm install && npm run build && cd ../Frontend && npm install && npm run build && cp -r dist ../bio-scholar-backend/frontend-dist",
        "start": "cd bio-scholar-backend && npm start"
    }
}
{
    "name": "desci-sentinel",
    "version": "1.0.0",
    "private": true,
    "description": "DeSci Sentinel monorepo — Backend + Frontend",
    "scripts": {
        "postinstall": "cd bio-scholar-backend && npm install && npm run build",
        "start": "cd bio-scholar-backend && npm start"
    }
}
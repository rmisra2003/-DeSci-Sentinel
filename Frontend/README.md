# DeSci Sentinel Frontend

This is the React + Vite frontend for Bio‑Scholar.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

By default the frontend expects the backend at `http://localhost:3001`.
